{
  "_comment": "Curated list of project ideas that have actually been built. This is maintained by hand (the daily routine never edits it). Two entry shapes are supported: (1) a 'ref' to an existing idea by {date, n} to promote it into the Implemented section, optionally overriding fields and adding url/repo; (2) a fully standalone project. See ideas/SCHEMA.md.",
  "projects": [
    {
      "ref": {
        "date": "2026-05-12",
        "n": 2
      },
      "url": "https://accessibility-lens-pisanuw.netlify.app/",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/accessibility-lens",
      "implementedOn": "2026-05-21"
    },
    {
      "ref": {
        "date": "2026-06-19",
        "n": 1
      },
      "url": "https://game-palette-inspector.netlify.app/",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/game-palette-inspector",
      "implementedOn": "2026-08-19",
      "notes": "Built in one session with zero runtime deps beyond React: verified Machado 2009 CVD matrices in linear sRGB, OKLab confusion detection, hue-preserving contrast fixes, PNG/print reports, 11 unit tests. Originally built in its own repo (pisanuw/c1) and mirrored to GitHub Pages; moved into Claude-capstone on 2026-08-27 with git subtree so history survived, with the Netlify site id pinned so the URL did not change. pisanuw/c1 is now an archived pointer and pisanuw.github.io/c1 redirects to the live app."
    },
    {
      "ref": {
        "date": "2026-07-26",
        "n": 1
      },
      "url": "https://pathfinding-playground-pisanuw.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/pathfinding-playground",
      "implementedOn": "2026-08-20",
      "notes": "Built in one session: five algorithms (BFS, DFS, Dijkstra, Greedy, A*) emitting a single event trace consumed by both the canvas renderer and the narrator. Narration is rule-based instead of the suggested Claude API calls, so it is free, offline, and always matches the algorithm state. Side-by-side compare mode, RLE share links in the URL hash, seeded maze generator, 20 vitest cases. Originally built in its own repo (pisanuw/pathfinding-playground); moved into Claude-capstone on 2026-08-27 with git subtree so history survived, with the Netlify site id pinned so the URL did not change. The old repo is now an archived pointer."
    },
    {
      "ref": {
        "date": "2026-08-20",
        "n": 1
      },
      "url": "https://schema-storyteller.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/schema-storyteller",
      "implementedOn": "2026-08-20",
      "notes": "Fully client-side schema explainer: paste SQL DDL, a Prisma schema, or JSON Schema and get a plain-English entity-relationship narrative, vocabulary suggestions for cryptic names, and a rule-based review of missing keys/indexes/constraints, exportable as Markdown. A hand-written parser and fixed rule set replace the idea's suggested LLM calls, so output is free, offline, and reproducible. 71 vitest tests, 88.88% coverage; lint/typecheck/build green. Deploy was blocked for 4 runs by a stale NETLIFY_AUTH_TOKEN repo secret; once the owner rotated it, the remaining failure was a deploy-runner bug (empty functionsDir resolved to the project root, so the CLI bundled the whole repo as functions). Fixed in deploy.mjs, deployed via GitHub Actions, and the CI runner verified the live page returns 200 and renders."
    },
    {
      "ref": {
        "date": "2026-08-13",
        "n": 1
      },
      "url": "https://shortcut-sprint.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/shortcut-sprint",
      "implementedOn": "2026-08-20",
      "notes": "Built in one session, fully client-side: SM-2 spaced repetition drills bundled shortcut sets for VS Code, Chrome DevTools, Figma, and Vim (multi-chord sequences like Ctrl+K Ctrl+S and ciw included), plus custom JSON set upload with strict validation. Key capture derives two candidate chords per keypress (physical event.code and produced event.key), so both Ctrl+Shift+[ and Vim's $ match; browser-reserved combos (Ctrl+W/T/N) are excluded since a web page cannot capture them. localStorage replaces the suggested Supabase backend, so there are no accounts and no server. Per-tool mastery radar chart and daily streak. 78 vitest tests, 98.6% statement coverage; lint/typecheck/build green. Deployed via the GitHub Actions deploy workflow (site created first try, page verified 200), siteId pinned afterward."
    },
    {
      "ref": {
        "date": "2026-08-17",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: parses two versions of SQL DDL (CREATE/ALTER/INDEX, Postgres and MySQL enums) or TypeScript interfaces into a shared IR, diffs them with rename detection for tables and columns, and classifies every change safe/caution/breaking with a one-sentence migration note; exports a Markdown PR checklist. A deterministic rule set replaces the suggested Claude API calls, so verdicts are instant, offline, and reproducible. 74 vitest tests, 97% statement coverage; lint/typecheck/build green. Deployed via the GitHub Actions deploy workflow (site created first try, page verified 200), siteId pinned afterward.",
      "url": "https://migration-diff-narrator.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/migration-diff-narrator",
      "implementedOn": "2026-08-20"
    },
    {
      "ref": {
        "date": "2026-08-11",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: a timed bug-spotting game where 1-3 defects are planted into realistic JavaScript/Python/SQL snippets and you click the lines you would flag in review. A seeded mutation engine (11 hand-written clean templates, 51 catalogued single-line defects with title, root cause, and fix) replaces the idea's suggested Claude API generation, and a date-seeded daily challenge plus a localStorage profile (per-category accuracy radar, streaks, best score) replace the Supabase leaderboard, so rounds are deterministic, offline, and free. Scoring rewards full sweeps: 100 per defect, -25 per false flag, time bonus only when everything is found. 46 vitest tests, 99.8% statement coverage; lint/typecheck/build green; end-to-end flow verified in headless Chromium. Deployed via the GitHub Actions deploy workflow (site created first try, page verified 200), siteId pinned afterward.",
      "url": "https://code-review-gauntlet.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/code-review-gauntlet",
      "implementedOn": "2026-08-21"
    },
    {
      "ref": {
        "date": "2026-08-16",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: a perceptual YIQ pixel diff with anti-aliasing detection (pixelmatch algorithm), grid-BFS region clustering, and a 7-type heuristic classifier (element-added/removed via local-background analysis, layout/spacing via a full-coverage displacement search, visibility via least-squares opacity fit, color via edge-map correlation, text via edge density) replace the suggested Claude Vision calls, so results are deterministic and screenshots never leave the browser. SVG overlay with filterable legend and per-region evidence, annotated PNG and standalone HTML report export, built-in sample pair covering all seven change types (verified end-to-end in headless Chromium). 56 vitest tests, 96.4% statement coverage; lint/typecheck/build green. Deployed via the GitHub Actions deploy workflow (site created first try, page verified 200), siteId pinned afterward.",
      "url": "https://ui-diff-lens.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/ui-diff-lens",
      "implementedOn": "2026-08-21"
    },
    {
      "ref": {
        "date": "2026-07-30",
        "n": 1
      },
      "notes": "Built in one session, fully client-side with zero runtime dependencies: a hand-written tokenizer, recursive-descent parser, three-valued-logic expression evaluator, and staged executor replay a SELECT one stage at a time (FROM, JOIN, WHERE, GROUP BY, HAVING, SELECT, DISTINCT, ORDER BY, LIMIT), showing the actual rows merging, being struck out, and collapsing into groups at every step. A deterministic narrator generated from real execution stats replaces the idea's optional Claude API narrator, and CSS animations replace Framer Motion, so explanations always match the screen and the app is free and offline. Honest SQL semantics (Kleene AND/OR, COUNT(x) vs COUNT(*), NULLs-last ordering), CSV paste with type inference, bundled demo tables and queries, URL-hash share links. 86 vitest tests, 99.7% statement coverage; lint/typecheck/build green. Deployed via the GitHub Actions deploy workflow (runner verified the live page returns 200), siteId pinned afterward.",
      "url": "https://sql-replay.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/sql-replay",
      "implementedOn": "2026-08-21"
    },
    {
      "ref": {
        "date": "2026-08-09",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: a deterministic word-to-DSP lexicon (20 base sounds, 18 modifiers with synonyms) replaces the idea's suggested Claude API code generation, and a pure sample-by-sample DSP engine (swept oscillators, seeded white/pink/brown noise, ADSR envelopes, RBJ biquad filters with block-rate sweeps, feedback echo, tanh drive, bit crush, peak normalization) replaces Web Audio graphs, so the browser just plays the rendered buffer and tests assert on the exact samples the user hears. Same description always yields the same sound (seed hashed from the words); a Variation button re-rolls only the seed. Oscilloscope, per-layer signal-chain view, tuning sliders (pitch/length/brightness/echo/volume), and 16/24-bit WAV export at 22050/44100/48000 Hz. 59 vitest tests, 100% statement coverage; lint/typecheck/build green; UI verified end-to-end in headless Chromium. Deployed via the GitHub Actions deploy workflow (site created first try, page verified 200), siteId pinned afterward.",
      "url": "https://sound-sketchpad.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/sound-sketchpad",
      "implementedOn": "2026-08-21"
    },
    {
      "ref": {
        "date": "2026-08-21",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: a forgiving HAR parser (skips malformed entries with warnings, wire-size fallbacks for Chrome/Firefox export quirks) feeds ten deterministic detectors (N+1 endpoint templates, cache-header audit, missing compression, redirect chains reconstructed hop by hop, serialized API waterfalls, duplicate fetches, 4xx/5xx groups, TTFB outliers, oversized payloads, HTTP/1.x connection churn) that replace the idea's suggested Claude API analysis, so reports are instant, reproducible, and the HAR never leaves the browser. Findings are ranked by severity and estimated impact, each with evidence and a copy-paste fix; SVG waterfall with phase segments, type filters, and per-finding highlighting; Markdown report export; a bundled synthetic session trips every detector. 56 vitest tests, 100% statement coverage; lint/typecheck/build green; verified end-to-end in headless Chromium. Deployed via the GitHub Actions deploy workflow: the first run failed because package-lock.json was gitignored at the repo root and npm ci had nothing to install from; force-added the lockfile, retriggered, site created and the runner verified the live page returns 200; siteId pinned afterward.",
      "url": "https://har-detective.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/har-detective",
      "implementedOn": "2026-08-23"
    },
    {
      "ref": {
        "date": "2026-08-25",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: paste code or name a concept, pick an audience (curious child, high school student, CS undergrad, non-technical adult), and get three analogies from three distinct everyday domains, each with a code-to-analogy mapping table. A curated corpus (47 concepts x 3 domains x 4 audience variants, 564 hand-written texts, ten browse groups) replaces the suggested Claude API generation, and a localStorage library with tags, search, and Markdown export replaces Supabase; read-only share links encode ids into the URL hash, so sharing needs no server. The detector combines keyword mentions with code-shape patterns and body-scoped self-call analysis (brace matching or Python indentation) for recursion; anything outside the 47 concepts gets an honest \"not detected\" instead of a hallucinated analogy. 125 vitest tests, 99.9% statement coverage; lint/typecheck/build green; UI verified end-to-end in headless Chromium. Deployed via the GitHub Actions deploy workflow (site created first try, runner verified the page returns 200), siteId pinned afterward. Follow-up same day: audience-toggle highlight fix and an example pull-down with one test-pinned snippet per concept (47), and staged corpus expansions from 14 to 25 to 47 concepts, adding systems, language mechanics, web/networking, and software-craft topics.",
      "url": "https://code-analogy-forge.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/code-analogy-forge",
      "implementedOn": "2026-08-25"
    },
    {
      "ref": {
        "date": "2026-08-02",
        "n": 1
      },
      "placeholder": true,
      "notes": "In progress since 2026-08-28"
    },
    {
      "ref": {
        "date": "2026-08-23",
        "n": 1
      },
      "notes": "Built in one session, fully client-side: a rule-based classifier (explicit section labels plus sentence-level cue patterns, each gene showing the evidence for its label) segments any pasted prompt into typed genes (role, persona, context, task, constraint, format, example), replacing the idea's suggested Claude API parsing; three hand-written deterministic mutations per gene type (e.g. Harden it / Soften it / Make it checkable, with case-aware word swaps like should-to-must) replace 'AI suggests three alternatives'; and a word-level LCS diff of the prompt itself replaces model-response diffing, since without API calls there are no responses to compare. A 12-rule lint scores the genome 0-100 (missing task, vague wording, brevity-vs-depth conflicts, negative-only constraints, duplicates, and more); genes can be edited, reclassified, reordered, and duplicated; a localStorage library keeps reusable genes with tags and search (replacing the implied server store); share links carry the whole genome in the URL hash; export as text, Markdown, or JSON. 87 vitest tests, 98.98% statement coverage; lint/typecheck/build green; verified end-to-end in headless Chromium. Deployed via the GitHub Actions deploy workflow (site created first try, runner verified the page returns 200), siteId pinned afterward.",
      "url": "https://prompt-genome.netlify.app",
      "repo": "https://github.com/pisanuw/Claude-capstone/tree/main/prompt-genome",
      "implementedOn": "2026-08-27"
    }
  ]
}
